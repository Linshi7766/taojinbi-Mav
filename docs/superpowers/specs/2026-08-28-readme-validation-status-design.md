# README 真机验证状态修正设计

## 目标

修正 README 中已经过时的“等待本轮真机验收”“真机验收尚未完成”等状态，同时准确区分三个公开模块已经取得的证据，避免把通用 dry-run 或单模块验收夸大为全部模块都已完整真机执行。

## 状态口径

- `搜一搜`：真机执行通过，金币到账已确认。
- `看看#`：真机执行通过，金币到账已确认。
- `发现精选好物`：离线与识别验证完成，尚待完整真机执行及到账验证。
- 公开 CLI 的只读 `--dry-run`：真机验收已经通过；只证明当前屏识别、脱敏报告、退出码和零 UI 动作边界。

一次真机成功记录不表述为所有设备、账号、任务刷新状态下的永久保证。

## README 修改范围

1. 更新“安全范围”三模块表的“发布状态”列，按上述模块证据分别标注。
2. 将总览中的“真机验收仍未完成”改为“只读 dry-run 已完成；精选好物完整执行仍待验证”。
3. 将“公开版运行合同”开头改为只读 dry-run 已验收通过，并保留真实 execute 需要人工确认与风控冷却要求。
4. 将 dry-run 命令说明从“等待授权验收”改为“需要时可复核”，继续明确它会连接 ADB/OCR，但不会点击、滑动或返回。
5. 不修改执行命令、支持白名单、任务策略、超时、退出码或隐私边界。

## 测试与验收

先扩展 `tests/test_readme_runtime_contract.py`，断言：

- README 不再包含两条过时状态短语；
- `搜一搜` 与 `看看#` 的状态包含“金币到账已确认”；
- `发现精选好物` 明确保留“待完整真机执行验证”；
- 只读 dry-run 明确写为“真机验收已通过”。

先运行新增测试并确认因旧 README 文案而失败，再修改 README 取得 GREEN，随后运行 README 契约测试和完整离线测试。只修改 README、测试和进度跟踪，不操作手机，不修改生产代码，不推送远程。

## 完成标准

- README 与本地真机/到账证据一致；
- 不夸大 `发现精选好物` 的验证状态；
- 旧状态短语由自动化测试防止回归；
- README 契约测试与完整离线套件通过；
- `git diff --check` 通过，`.workbuddy/` 保持未触碰。
